{
  "name": "personal-site",
  "install": "npm ci --no-audit --no-fund && (npx playwright install --with-deps chromium || npx playwright install chromium || true)",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev",
      "description": "Next.js dev server on http://localhost:3000"
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
