# personal-site

Personal site — slzavaletta.com. Next.js (App Router) + Tailwind, deployed on Vercel.

## Dev
npm install
npm run dev      # localhost:3000

## Build
npm run build

Pushes to `main` auto-deploy via Vercel.
